# Inngest environment template
# Copy this file to `.env` and adjust values for your environment.

# Enable dev mode behavior in src/demo/server/inngest.ts.
# 1/true enables local defaults; 0/false disables them.
INNGEST_DEV=1

# Inngest server URL.
# Local Docker default from docker-compose: http://localhost:8288
INNGEST_BASE_URL=http://localhost:8288

# Event key used when sending events to Inngest.
# Local dev default is "dev"; replace in non-dev environments.
INNGEST_EVENT_KEY=dev

# Signing key used by the Inngest serve endpoint in non-dev environments.
# Leave unset for local dev; set from your Inngest app settings for production.
# INNGEST_SIGNING_KEY=signkey-prod-xxxxxxxx
